# .init.iocsh
# ===========

mrmEvgSetupPCI($(DS), $(PCIID))

dbLoadRecords("evm-mtca-300-ess.db", "SYS=$(AS), D=$(DS), EVG=$(DS), FEVT=$(TS_CLK), FRF=$(TS_CLK), RFDIV=1")

var evrMrmTimeNSOverflowThreshold 1000000

#EOF
